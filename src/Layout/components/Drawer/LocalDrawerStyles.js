import {makeStyles} from "@material-ui/core";

const localDrawerStyles = makeStyles(theme => ({
        drawer: {
            width: 240,
            flexShrink: 0,
        },
    })
);

export default localDrawerStyles;
